# 用户提问原图档案

本目录保存用户在提问时附带、且与知识解释直接相关的原始图片。原图不是装饰，也不能被重新绘制的 Mermaid 代替：问题档案和对应章节都会嵌入同一份副本，并说明应该怎样读图。

用户提供图片不自动等于它具有开放许可。本目录只记录“用户提供”这一来源事实，不为第三方截图擅自添加许可。是否提交到公开仓库，仍以用户在当次任务中的明确授权和已知权利边界为准。

## Q002：两组 `LOAD / ADD / STORE` 教学指令截图

- 文件：[q002-load-add-store-screenshot.png](q002-load-add-store-screenshot.png)
- 来源：用户在 Q002 提供，2026-08-22。
- 本仓库处理：原样保存，SHA-256 为 `6B1C37640C0F11587C3E20B2DA94588638551ECD7A2BACD8A938C932EFC317CB`。
- 图中可观察到：上半段为 `12 + 20`，下半段为 `100 + 35`；两段都用 `LOAD / ADD / STORE` 表达“读两个数、相加、写入 `a`”的教学模型。
- 教学边界：图中的 `LOAD`、`[a]` 和寄存器名字属于视频或教材的抽象记号。它们不是本仓库的操作命令，也不能直接推断任何真实 ISA 的完整语法或机器码。
- 对应正文：[第 2 章第 2 节](../../docs/02-cpu-context-registers-and-instructions.md#2-先读懂你截图中的指令)；对应问题记录：[Q002](../../questions/README.md#q002cpu进程上下文寄存器并发与指令)。

# 操作系统个人知识库维护指令

## 目标

本仓库用于根据用户的真实提问，逐步建立一套属于用户自己的操作系统知识体系。用户的提问是选择学习主线、判断概念断点和决定详细程度的首要依据。

维护本仓库时，必须遵守本文件。不要因为某个主题很重要，就在用户尚未接触时自行写成完整教程。

## 一、始终从用户问题出发

1. 先保留用户原始问题的原意，再开始整理答案。
2. 将一个问题拆成：
   - 用户明确询问的名词或过程；
   - 容易混淆的对象；
   - 概念之间缺失的连接；
   - 可以用于确认理解的情境题。
3. 对用户知识状态只能写“从本次问题推断的待澄清点”，不能把推断写成对用户能力的定论。
4. 新问题必须登记到 `questions/README.md`，并链接到对应知识章节。
5. 如果新问题属于已有主题，优先补充原章节；只有形成新的稳定主题时才创建新章节。

## 二、控制知识扩展边界

1. 用户已经提问的内容可以详细展开。
2. 用户尚未提问的内容，只在 `docs/00-operating-system-knowledge-map.md` 中保留：名称、作用和与已学知识的连接。
3. 不提前加入大段算法、实现细节、历史背景或语言教程。
4. 不添加与知识整理无关的程序、依赖、构建工具、网页或自动化系统。
5. 不为了“看起来完整”创建空章节或空目录。

## 三、每个详细主题的固定结构

详细章节至少包含：

1. 本章来自哪些原始问题；
2. 学完后应该能够回答什么；
3. 一句话定义；
4. 为什么需要这个概念；
5. 它与已有概念怎样连接；
6. 运行过程中怎样变化；
7. 至少一幅具有解释作用的 Mermaid 图；
8. 一个可以追踪对象或状态变化的实例；
9. 常见误区和类比边界；
10. 不靠背诵定义的情境自测题及折叠答案；
11. 返回知识地图、问题档案和相关章节的链接。

图必须用于解释层级、关系、时序、状态或数据流，不能只作为装饰。图后必须补充文字说明，使读者即使暂时看不到 Mermaid 渲染也能理解核心结论。

## 四、必须严格区分的层次

在任何章节中，都要主动检查是否混淆了以下对象：

- 源代码、编译产物、程序和一次运行实例；
- 程序、进程和线程；
- 虚拟地址空间、物理内存和磁盘文件；
- 可执行文件中的 section、加载后的 segment 或内存映射区；
- 磁盘文件、内核打开文件对象、句柄或文件描述符、应用缓冲区；
- 编程语言规范、某种常见实现、语言运行时和操作系统；
- 用户态操作、系统调用和内核态工作；
- 内容相同与对象身份相同。

## 五、准确性与限定条件

1. 对“通常”“常见实现”“教学示意图”明确写出限定条件。
2. 不得把以下简化说法写成绝对事实：
   - 程序启动时完整复制进物理内存；
   - 局部变量一定在栈上；
   - 堆一定向高地址增长，栈一定向低地址增长；
   - 可执行文件 section 与运行时内存区域严格一一对应；
   - C 和 Rust 永远只有提前编译，Python 和 JavaScript 永远只有解释执行；
   - JavaScript 使用事件循环就意味着没有线程；
   - 内容相同的两个文件就是同一个文件。
3. Windows、Linux、CPU 架构、编译器或运行时存在差异时，要标明讨论的层次和适用范围。
4. 类比用于建立直觉，但必须说明类比在哪些地方不再成立。
5. 不确定或尚未验证的内容不能写成已确认事实。

## 六、同步更新规则

每次增加或修改实质知识后，同步检查：

- `README.md` 的导航和当前学习主线；
- `docs/00-operating-system-knowledge-map.md` 的节点与状态；
- `questions/README.md` 的问题、待澄清点、链接和复习状态；
- 新旧章节之间的双向链接；
- Mermaid 图和正文是否表达同一关系；
- 新内容是否越过了用户当前提问边界。

## 七、Git 与发布边界

1. 文档更新不自动等于允许提交或推送。
2. 只有用户在当前任务中明确要求时，才执行提交、推送或远程仓库修改。
3. 提交时只暂存本次授权的文件，不能夹带无关修改。
4. 推送后分别验证本地文件、提交内容、远程分支和公开页面；不能只凭命令退出码宣布成功。
